# 更新日志

本文件记录 `astrbot_plugin_xwdraw` 的版本变更。日期为服务端联调验证当天。

## [0.4.0] - 2026-07-31

对接 `sd.loping151.com` 服务端 v1.3.6 全量重做。用真实密钥把 openapi 里的每个接口跑了一遍，
并对 40 张真实图片的 metadata 做了审核阈值校准。

### 破坏性变更

- **移除全部视频功能**。服务端已于 2026-06-30 公告「视频生成已下线」，openapi 中 `/api/video/*`
  已不存在。删除 `视频生成`、`视频历史`、`视频查看`、`视频缩略图`、`视频末帧`、`视频源图`、
  `视频开关` 七个命令，以及 `video_enabled` 配置项。
- **`r18_fail_without_metadata` 更名为 `r18_block_without_score`**。语义由「有没有 metadata」
  改为「有没有拿到分类器分数」——前者永远为真（metadata 总是带 `is_r18`），实际从未生效过。
- **`r18_nsfw_score_threshold` 默认值由 `0.65` 改为 `0`**（`0` = 跟随 `r18_policy` 档位）。
  升级后请手动把配置里残留的 `0.65` 改回 `0`，否则会大量误拦（详见下方校准数据）。
- `绘图状态` 不再是 `绘图开关` 的别名，改为独立的完整状态卡片。

### R18 / R18G 审核重做

对服务端 40 张真实图片取 metadata 校准后，发现旧实现在两个方向同时失效：

**漏放**——`is_r18` / `is_r18g` 是用户生成时自己勾选的标记，不是内容检测结果。样本中存在
提示词为「色图」、`nsfw_score=0.997`、但 `is_r18=false` 的图片。旧实现把 `is_r18=false`
当作安全依据直接放行。

**误拦**——`nsfw_score` 的真实分布是：

| 内容 | 分数区间 |
| --- | --- |
| 明确安全（角色立绘、Q 版、场景） | 0.000 ~ 0.480 |
| 擦边（泳装、比基尼、普通立绘） | 0.683 ~ 0.843 |
| 明确 R18 | 0.988 ~ 0.999 |

旧默认阈值 `0.65` 落在安全区内部，连「1girl, standing, smile」（实测 0.695）都会被拦下。

变更：

- 新增 `r18_policy` 策略档位：`strict` 0.80 / `standard` 0.90（默认）/ `loose` 0.97 / `off`。
  阈值落在 0.85 与 0.98 之间的干净间隔里。`r18_nsfw_score_threshold` 大于 0 时覆盖档位。
- 自述标记 `is_r18` / `is_r18g` 只用于拦截，不再作为放行依据。
- 新增提示词预筛：内置中英文 R18 / R18G 词表，命中时在提交远端之前拒绝，不消耗额度；
  若当前会话已放行，则自动带上对应的 `is_r18` / `is_r18g` 标记提交，让画廊标记保持正确。
  可用 `r18_extra_blocklist` 追加自定义词条。
- 生成响应里没有 `nsfw_score`，改为带重试拉取 `/api/image-metadata`（评分可能比生成稍晚落库）。
- 拿不到分数时判定为 `unknown` 而非 `safe`，由 `r18_block_without_score` 决定是否保守拦截。
- 修复外部审核漏放：响应里同时缺少 `safe` / `level` / `score` 时旧实现会走到「放行」分支，
  现在按无效响应处理，遵循 `external_review_fail_closed`。
- 放行不等于安全：被会话放行的高分图片在审核日志里仍如实记为 `r18`。
- 新增审核结论缓存（按图片哈希 + 全部判定输入做键）与审核日志 `绘图审核`。
- 审核日志记录**会话 ID 与触发者 ID**，命中缓存的复审同样单独记一笔，
  这样同一张图被不同群、不同人再次触发时能各自归因。后端 `logger.warning`
  也带上 `session=` / `sender=`。

### 新增：版本形态与分级放行

- 新增 `edition` 配置项：`shared`（共用版，默认）/ `custom`（定制版）。
- 新增 `custom_edition_allow_r18`（默认 `true`）与 `custom_edition_allow_r18g`（默认 `false`）。
  定制版默认放行 R18 这一行为现在是一个可以单独关掉的开关，R18G 需另行开启。
- 新增会话级指令 `R18开启 [含G]` / `R18关闭` / `R18状态` / `R18跟随`（仅 Bot 管理员），
  状态持久化到插件数据目录的 `switches.json`。
- 放行优先级：会话级指令 > 版本形态 > `r18_allowed_session_ids` 名单 > 全部拦截。
  名单只放行 R18，R18G 必须由管理员显式开启。

### 新增：服务端能力适配

- **NovelAI**：识别提示词中的 `nai` / `nai1` / `-m 90` / `-m 91`，提交提示会标注走独立 NAI 额度；
  `绘图账号` 与 `绘图状态` 分别显示本地额度与 NAI 额度。
- **多参考图**：一条消息里最多取 4 张图，按服务端约定提交 `images` 数组（裸 base64，无 `data:` 前缀）
  与 `nai_ref_mode`。新增 `--vt`（风格参考）/ `--pr`（角色参考）/ `--i2i`（整图重绘，默认）。
- **新命令**：`绘图模型`（按架构分组列出模型与参数范围）、`绘图版本`（服务端版本）、
  `预设搜索 <关键词>`（跨角色/翻译表/风格/服装/用户预设匹配全部别名并标注架构）、
  `画廊删除 <日期>/<文件名>`、`绘图审核 [数量]`。
- **生成反馈**：展示服务端 `matches` 命中的角色/风格/服装，以及 `arch_dropped` 架构冲突丢弃提示。
- **画廊**：改为 Bearer 直连，仅在 401/403 时回退到 `/gallery/login` 建会话。
- **文档**：修正为服务端实际接受的四个名字（`常规法典` / `色色法典` / `花花魔法书` / `涩涩词条大全`）。
  其中「涩涩词条大全」是 77MB 的全量字典，改为回复链接而不是下载。
- `-d` 默认值由 `0.7` 改为 `0.6`，与服务端 `GenerateRequest` 一致。
- `timeout` 默认值由 `60` 提升到 `90`。

### 帮助与展示

- 帮助改为卡片式分章节：`绘图帮助` 总览，以及 `参数` / `nai` / `预设` / `审核` / `管理` 五个子章节。
- 按权限分层：普通用户、群管理员、Bot 管理员看到不同内容，`绘图帮助 管理` 需要 Bot 管理员。
- `绘图状态` 里的服务地址对非 Bot 管理员脱敏（`sd.*********.com`），只有 Bot 管理员看到完整地址。
  `绘图文档`（含 77MB 字典的直链）保持 Bot 管理员专用。
- `绘图状态`、`绘图账号`、`绘图公告`、`推荐提示`、`最近图片`、`画廊筛选`、`我的预设`、
  `绘图文档` 全部改为统一的卡片排版。
- 预设列表与搜索结果显示全部别名与所属架构。

### 测试

- 测试从 38 项扩充到 69 项，覆盖提示词预筛、阈值校准边界（用实测分数做断言）、
  自述标记与分数的优先级、版本形态与会话级放行的组合、外部审核的各种畸形响应、
  metadata 重试、画廊免登录直连、权限分层与信息泄露。

## [0.3.3]

- 新增视频功能分控开关 `video_enabled` 与会话级 `视频开启` / `视频关闭`
- 美化视频任务提交回复
- 生成成功后用合并转发消息回传服务端的实际提示词

## [0.3.2]

- 重写帮助菜单，按普通用户、群管理员、Bot 管理员分层展示
- 将账号/配额、生成配置、队列、图库、metadata、标签、视频取回、文档等维护命令收口为 Bot 管理员专用
- 白名单外敏感标记会在生成前拒绝，不请求远端
- 增强图生图、视频生成和外部审核测试

## [0.3.0]

- 重构 API client、生成结果解析、图片发送和 R18 自审流程
- 新增 R18/R18G metadata 审查和可插拔外部审核接口
- 补齐账号、生成配置、公告、队列、推荐提示、画廊、图片标签、视频、文档等接口命令
- 修复 README 中存在但代码缺失的 `生成配置` 命令
- 替换需要真实 token 的测试脚本为本地无网络测试

## [0.2.0]

- 新增预设管理功能
- 新增自定义预设功能
- 新增帮助系统
- 修复图片下载地址问题

## [0.1.0]

- 初始版本
- 支持基础文生图和图生图
